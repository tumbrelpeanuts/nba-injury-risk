## Requirements
- [uv](https://docs.astral.sh/uv/getting-started/installation/)

## Setup
clone the repo, then:

    uv sync

That's it — uv will read `.python-version` and `pyproject.toml` to set up the right Python version and install dependencies automatically.
